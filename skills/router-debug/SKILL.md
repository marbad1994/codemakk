---
name: router-debug
description: Use when debugging the Cline model router, provider execution, streaming, fallbacks, token logging, or task context.
x-router:
  profile: deep
  outputMode: answer
  allowScripts: false
---

# Router Debug

Focus on router architecture, provider behavior, streaming, fallback chains, token logging, task context, and OpenAI-compatible API behavior.

Prefer concrete file-level fixes.
